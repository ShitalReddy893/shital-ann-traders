server {
    listen 80;

    root /usr/share/nginx/html;
    index index.html;

    # Redirect root to /ecommerce if needed
    location / {
        return 302 /ecommerce/;
    }

    # Serve Angular SPA at /ecommerce
    location /ecommerce/ {
        try_files $uri $uri/ /index.html;
    }
}
